{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build -w backend"
  },
  "deploy": {
    "startCommand": "npm run start -w backend",
    "healthcheckPath": "localhost:3000/healthcheck"
  }
}
